# 웹 프레임워크
fastapi>=0.115.0,<1.0.0
uvicorn[standard]>=0.32.0,<1.0.0
python-multipart>=0.0.12,<1.0.0
jinja2>=3.1.4,<4.0.0

# HTTP 클라이언트
httpx>=0.27.0,<1.0.0
curl_cffi>=0.7.4,<1.0.0

# 데이터베이스
sqlalchemy>=2.0.43,<3.0.0
psycopg[binary]>=3.2.0,<4.0.0

# 크롤링
requests>=2.32.3,<3.0.0
beautifulsoup4>=4.12.3,<5.0.0
selenium>=4.27.0,<5.0.0
brotli>=1.0.9,<2.0.0  # Brotli(br) 압축 지원 - investing.com 필수 (Docker 환경 통일)

# 스케줄링
apscheduler>=3.10.4,<4.0.0
redis>=5.0.0

# 타임존 (항상 최신 권장)
pytz>=2024.2

# 로깅
python-json-logger>=2.0.7,<3.0.0

# 환경 변수 관리
python-dotenv>=1.0.1,<2.0.0

# 시스템 모니터링
psutil>=7.0.0

# Yahoo Finance (DXY 폴백 + 히스토리 백필)
yfinance>=0.2.40,<1.0.0

# Firebase (Phase 2 - FCM 푸시 알림)
firebase-admin>=6.5.0,<7.0.0
