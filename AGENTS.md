# Repository Guidelines

이 문서는 Codex 에이전트가 프로젝트 전용 지침을 빠르게 찾도록 돕는 내비게이터다. 모든 세부 내용과 정책은 `CLAUDE.md`에 있으므로, 아래 순서를 따라 해당 문서를 먼저 확인한다.

## 사용 순서
1. `CLAUDE.md`의 **프로젝트 개요**와 **핵심 아키텍처**를 읽어 시스템 흐름을 파악한다.
2. 같은 문서의 **주요 문서 가이드**에서 필요한 러프북(DECISIONS, CRAWLERS, CHECKLIST 등)을 골라 참고한다.
3. 전역 Codex CLI 규칙은 `~/.codex/AGENTS.md`에 정리되어 있으니, 계획 수립·테스트·승인 정책은 그 문서를 따른다.

## CLAUDE.md에서 꼭 볼 섹션
- **프로젝트 개요 & 아키텍처**: 크롤러→스케줄러→DB→WebSocket 파이프라인 설명.
- **MCP 설정**: 사용 가능한 서버, 스코프 구분, local scope 사용상의 주의사항.
- **스케줄링 시스템**: 4단계 모드 전환 조건과 크롤러 활성/비활성 규칙.
- **데이터 관리 정책**: 보관 주기, 중복 저장 방지 전략.

## 작업 체크리스트
- 변경 전후로 `CLAUDE.md` 링크된 문서(예: `DECISIONS.md`, `DOCKER.md`)가 최신인지 확인하고 필요시 갱신한다.
- 테스트·빌드·배포 명령은 전부 `CLAUDE.md` 해당 섹션을 따른다; 이 파일에는 상세 명령을 반복 기입하지 않는다.
- 새로운 규칙이 생기면 `CLAUDE.md`에 추가하고, 여기에는 “해당 섹션 참조” 문구만 덧붙인다.
