# /etc/systemd/journald.conf.d/limits.conf 로 설치 (정본은 이 파일).
# 2026-07-26 실측: 상한이 없어 journal이 892MB까지 증가했다 (정리 후 92MB).
# 일회성 정리는 `journalctl --rotate --vacuum-size=100M` — `--rotate`가 없으면 archived만 대상이라
# active journal이 남는다.
[Journal]
SystemMaxUse=200M
SystemKeepFree=2G
